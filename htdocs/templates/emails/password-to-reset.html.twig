<!doctype html>
<html lang="{{ app.request.locale }}">
<head>
    <meta charset="UTF-8">
</head>
<body>
{% set uri = 'http://' ~ url ~ '/api/customers/' ~ id ~ '/reset-password/' ~ email ~ '/' ~ token %}
<p>{{ 'password_to_reset.content %uri%'|trans({'%uri%': uri}, 'emails')|raw }}</p>
</body>
</html>